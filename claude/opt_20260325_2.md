# 第2步
v1 项目文件比较乱
- 根目录中包含了生成go、dart代码的.yaml文件
- 子目录下还包含了生成http接口所需的脚本文件等

该工程多端共用，且需共享给外部团队使用，所以，优化目标还需要：整洁、专业
1. 项目根目录下仅包含基础的buf.yaml、Makefile。
2. 生成目标语言代码的.yaml文件放入专门的路径管理
3. 生成http接口所需的文件放入专门的路径管理
4. 使 hi 路径中，尽可能只包含proto文件。
5. 生成的目标语言代码应排除在git同步内容外。

先判断优化目标是否可实现，再进行优化。